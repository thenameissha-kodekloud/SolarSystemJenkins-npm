pipeline {
    agent any

    stages {
        stage('VM Node version and npm version') {
            steps {
                bat 'node -v'
                bat 'npm -v'
            }
        }
    }
}