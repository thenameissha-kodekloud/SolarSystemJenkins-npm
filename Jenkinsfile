pipeline {
    agent any

    stages {
        stage('VM Node version and npm version') {
            steps {
                sh 'node -v'
                sh 'npm -v'
            }
  
            }
        }
    }   
}